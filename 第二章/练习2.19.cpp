/**
 * 引用:
 *     引用的本质就是别名，不产生新的对象
 *     1. 引用必须在申明的时候初始化;
 *     2. 引用初始化后无法更改;
 * 
 * 指针:
 *     指针表示的是间接访问，指针本身是一个新的对象，只不过该对象内部存储的是所指向对象的地址
 *     1. 指针可以申明的时候不进行初始化，后续在进行初始化;
 *     2. 指针初始化后可以更改;
 * 
 */